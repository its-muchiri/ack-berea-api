{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/mpesa/query/(.*)", "dest": "/api/mpesa/query.js" },
    { "src": "/api/(.*)", "dest": "/api/$1.js" }
  ]
}
